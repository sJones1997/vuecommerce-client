import { createStore } from 'vuex'

import LoginModule from './modules/LoginModule'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    LoginModule
  }
})
